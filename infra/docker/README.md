# Infra Docker

Ponto de organização para artefatos Docker específicos por ambiente.

Nesta fase, o `docker-compose.yml` raiz já fornece o ambiente local com:

- MySQL 8
- Backend Spring Boot
- Frontend Next.js
